node_modules/
dist/
coverage/
.env
.env.local
*.tsbuildinfo
.cache/
.DS_Store

# Eval run artefacts. The cases and the scoring are committed; the results of
# any particular run are not — they are per-key, per-model and per-day.
evals/results/
